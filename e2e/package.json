{
  "name": "garageops-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright E2E tests for GarageOps. Isolated under /e2e so its Node toolchain never collides with the package.json Vaadin Flow generates at the repo root during the Maven build.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen http://localhost:8080"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.0"
  }
}
